
Environment Preparation:
- Find DB creations script in “DB Files” and install tables in new DB (Tested on MS SQL Server) 
- Update connection string accordingly  such as host and new db name.
- Use this connection string  ->  <add name="DBTextAnalyzerEntities"  …  

Note:
Without Database application will output to console JSON only and you should see exception related with DB update.
